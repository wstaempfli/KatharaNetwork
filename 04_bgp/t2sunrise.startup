#!/bin/bash

ip address add 10.0.3.2/30 dev eth0	# to t1att
ip address add 10.0.8.1/30 dev eth1	# to t3eth
ip address add 10.0.9.1/30 dev eth2	# to t3zkb

# append the ruleset to the config file
cat /shared/ruleset.conf >> /etc/frr/frr.conf

systemctl start frr

sleep 5 && vtysh -c "show ip bgp"