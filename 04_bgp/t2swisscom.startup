#!/bin/bash

ip address add 10.0.1.2/30 dev eth0	# to t1att
ip address add 10.0.4.1/30 dev eth1 # to t2switch
ip address add 10.0.5.1/30 dev eth2	# to t3ubs

# append the ruleset to the config file
cat /shared/ruleset.conf >> /etc/frr/frr.conf

systemctl start frr

sleep 5 && vtysh -c "show ip bgp"