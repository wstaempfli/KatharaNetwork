#!/bin/bash

ip address add 10.0.5.2/30 dev eth0		# to t2swisscom
ip address add 10.0.101.1/24 dev eth1	# local network of t3ubs

# append the ruleset to the config file
cat /shared/ruleset.conf >> /etc/frr/frr.conf

systemctl start frr

sleep 5 && vtysh -c "show ip bgp"